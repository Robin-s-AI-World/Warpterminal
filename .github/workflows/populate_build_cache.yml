# Personal-fork safety: manual only. Do not consume Actions on pushes or schedules.
on:
  workflow_dispatch:

name: Populate Build Cache

jobs:
  populate_build_cache:
    name: CI
    if: github.repository_owner == 'warpdotdev'
    uses: ./.github/workflows/ci.yml
    secrets: inherit
